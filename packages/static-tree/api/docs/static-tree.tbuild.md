<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Home](./index.md) &gt; [static-tree](./static-tree.md) &gt; [tBuild](./static-tree.tbuild.md)

## tBuild() function

Build helper that uses [ExtendedTNodeBuilder](./static-tree.extendedtnodebuilder.md) internally.

<b>Signature:</b>

```typescript
export declare function tBuild<Key extends string, ChildrenRecord extends Record<string, ExtendedTNode>, Data extends TNodeData>(builder: ExtendedTNodeBuilder<Key, ChildrenRecord, Data>): TBuildOutput<Key, ChildrenRecord, Data>;
```

## Parameters

|  Parameter | Type | Description |
|  --- | --- | --- |
|  builder | [ExtendedTNodeBuilder](./static-tree.extendedtnodebuilder.md)<!-- -->&lt;Key, ChildrenRecord, Data&gt; |  |

<b>Returns:</b>

[TBuildOutput](./static-tree.tbuildoutput.md)<!-- -->&lt;Key, ChildrenRecord, Data&gt;


## Remarks

Along with a built [ExtendedTNode](./static-tree.extendedtnode.md)<!-- -->, `tBuild` outputs an [ExtendedTNodeBuilder](./static-tree.extendedtnodebuilder.md) for advanced use cases, see examples for more details.

## Example 1

Typical usage

```typescript
import { tBuild } = 'static-tree';

const { node } = tBuild('root', {
 build: (builder) => builder
   .addData({ some: 'root data' })
   .addChild('leaf')
   .addChild('child', {
     build: (builder) => builder.addChild('grandChild', {
       build: (builder) => builder.addData({ some: 'grand child data' }),
     }),
   }),
});

node.$.data().some // -\> 'root data';
node.child.grandChild.$.data().some // -\> 'grand child data';
node.child.grandChild.$.path() // -\> 'root/child/grandChild';
```

## Example 2

Use `tBuild` builder output in nested `ExtendedTNodeBuildCallback`

```typescript
const { builder: externalBuilder } = tBuild('external', {
  build: (builder) => builder.addChild('child').addData({ some: 'data' }),
});

const { node } = tBuild('root', {
  build: (builder) => builder.addChild(externalBuilder),
});
```

